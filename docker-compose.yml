version: '3'
services:
  bot:
    container_name: multiSVBot
    build: .
    depends_on:
      - "db"
    ports:
      - 8080:8080

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres